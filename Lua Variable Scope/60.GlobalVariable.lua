--Global variable by default. This means the variable can be accessed and used from anywhere in your script.

--Here's how you create a global variable:

playerName = "Alex"
score = 100

--[[Both playerName and score are global variables because they were declared without the local keyword. Once created, these variables can be 
accessed from any part of your program - inside functions, loops, or anywhere else in your script.]]